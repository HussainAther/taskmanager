import React from 'react';
import ReactDOM from 'react-dom';
import TaskList from './components/TaskList'; // Assuming TaskList is your main component

ReactDOM.render(
  <React.StrictMode>
    <TaskList />
  </React.StrictMode>,
  document.getElementById('root')
);

